# ===== ????? =====
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:hospital-outpatient}?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:020121}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== MyBatis =====
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=com.ncst.hospitaloutpatient.model.entity
mybatis.configuration.map-underscore-to-camel-case=true

# ===== JWT =====
jwt.secret=${JWT_SECRET:bIdOxoy3hvDLO4AGezynFn+bfpZ1blLP0hrsX/1NYGk=}

# ===== ???? =====
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# ===== ?? =====
server.port=8080